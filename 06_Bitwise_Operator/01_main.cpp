#include <iostream>
using namespace std;

int main()
{
  // int a = 3, b = 7;
  // cout << "Bitwise Operator" << endl;
  // cout << (a & b) << endl;  // AND Operator
  // cout << (a | b) << endl;  // OR Operator
  // cout << (a ^ b) << endl;  // XOR Operator
  // cout << (a << b) << endl; // Left Shift Operator
  // cout << (a >> b) << endl; // Right Shift Operator

  cout << (10 << 2) << endl;
  cout << (10 >> 1) << endl;

  return 0;
}